# Mesh utilities

Validate and optionally repair meshes before MCFS. `MeshManager` is optional
application glue; most callers use `load_and_repair` or a plain `trimesh`.

::: pymcfs.validate.validate_mcfs_mesh

::: pymcfs.validate.load_and_repair

::: pymcfs.mesh.MeshManager

::: pymcfs.mesh.example_mesh
